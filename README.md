# dogfood-hub

The interactive **design hub** for Dogfood — a tiny desktop forge where you prompt
Claude Code to build a UI piece and watch it render live in the real repo.

Single self-contained `index.html`. Component inventory (current + future), decisions,
architecture, and the visual system. Long-form docs live separately (vocs).

Published via GitHub Pages.
